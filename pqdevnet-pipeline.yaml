# PQ Devnet Pipeline Configuration
# Fork-specific config for PQ Devnet notebooks (separate from upstream pipeline.yaml)

version: "1.0"

# Notebook Registry
notebooks:
  - id: 01-pq-signature-performance
    title: PQ Signature Performance
    description: Post-quantum cryptographic signature timing analysis
    icon: Key
    source: notebooks/pqdevnet/01-pq-signature-performance.ipynb
    order: 1

# Settings
settings:
  data_dir: notebooks/data
  rendered_dir: site/rendered
  # prometheus_url: Set PROMETHEUS_URL environment variable
